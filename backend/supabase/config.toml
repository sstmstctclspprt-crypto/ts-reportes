# Configuración de Edge Functions (Supabase CLI).
# La función valida Authorization de forma explícita; dejamos verify_jwt desactivado
# para evitar bloqueos de preflight/CORS en navegador.
[functions.generate-ctpat-pdf]
verify_jwt = false

[functions.purge-expired-ctpat-data]
verify_jwt = false
