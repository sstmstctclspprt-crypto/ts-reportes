# Configuración de Edge Functions (Supabase CLI).
# Zero Trust: la puerta debe exigir JWT válido y la función mantiene validaciones defensivas adicionales.
[functions.generate-ctpat-pdf]
verify_jwt = true
